 CREATE TABLE users (
                       id_user integer PRIMARY KEY AUTOINCREMENT,
                       user_name VARCHAR(100),
                       password VARCHAR(20),
                       weight_goal real
                   )
